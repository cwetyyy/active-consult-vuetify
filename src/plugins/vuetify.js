import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify); //Like this we register Vue as a plugin in our app

export default new Vuetify({
    // icons: {
    //     iconfont: 'md', // 'mdi' || 'mdiSvg' || 'md' || 'fa' || 'fa4' || 'faSvg'
    //   },
});
